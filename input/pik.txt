class t {
    a = [1,2,3]
    fn req(self) {
        if self.a.len() > 0 {
            self.a.remove(self.a.len()-1);
            self.req();
        }
        return;
    }
}

a = t();
b = a.req();
zzz = a.a.len();
w = 1 + (5/2) + (55*5) / 5 / (2**2);
